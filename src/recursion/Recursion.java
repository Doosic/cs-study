package recursion;

public class Recursion {

  /**
   * 재귀함수는 반복적으로 자기 자신을 호출하는 것이다.
   *
   * - 재귀의 첫 번째 패턴
   *   단순히 반복실행 하는 것. 그러나 단순히 반복실행만 한다면
   *   for문을 사용하는 것만 못하다. stack의 공간을 차지하기 때문이다.
   *
   * - 하위 문제의 결과를 기반으로 현재 문제를 계산하는 패턴
   *   for문을 사용하는 것은 상향식 계산이라고 한다.
   *   재귀를 사용하는 것은 하향식 계산이라고 한다. 그러나 모두 하향식은 아니다.
   *   ex) 하향식 구현: 이 전까지 계산된 모든 값을 현재에  계산하는 것
   *
   *   상향식은 모두 구현이 가능하나, 하향식 구현은 재귀로만 구현할 수 있다.
   * */
}
